/**
 * @rankpilot/shared
 *
 * Single entry point for types and constants shared between the
 * Next.js web app (`apps/web`) and any TypeScript tooling that
 * needs to stay in sync with the FastAPI backend (`apps/api`).
 */
export * from "./types";
export * from "./constants";
